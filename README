#Build Instructions 

Go into the build directory and choose the folder corresponding to the
build type you want.  Enter the folder and run ./build.

#Build Dependencies
In ubuntu install:

- libpoppler-glib-dev
- lib-gtkmm2.4-dev
- bison
- flex
- cmake

Note need Cairo version >= 1.10.2 (If using Ubuntu this means version >= 12.04 is needed).
